{
  "name": "f1race",
  "version": "0.1.0",
  "private": true,
  "description": "F1 online arcade + RPG. Monorepo: race-engine, server, web/mobile clients.",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "sim": "pnpm --filter @f1race/race-engine sim",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
